# Kids2gether_WIX

Site estático com a página de redirecionamento `ninapelomundo_redirect.html`.

Instruções rápidas:

- Para publicar localmente: abra `ninapelomundo_redirect.html` no navegador.
- Para publicar no Vercel: usar Vercel CLI (`vercel`) ou conectar o repositório no painel Vercel.
